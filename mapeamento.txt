# Mapeamento de botoes gerado pelo KeyMapper
# Dispositivo: retrogame_joypad

# Use estes valores em love.gamepadpressed(_, button) ou love.gamepadaxis(_, axis, value)
# Valores prefixados com 'axis:' sao capturados via gamepadaxis

button_a = b
button_b = a
button_x = x
button_y = y
button_start = start
button_select = back
button_l1 = leftshoulder
button_l2 = axis:triggerleft
button_r1 = rightshoulder
button_r2 = axis:triggerright
lstick_click = leftstick
lstick_x = axis:lefty
lstick_y = axis:lefty
rstick_click = rightstick
rstick_x = axis:rightx
rstick_y = axis:rightx

# D-pad (confirmado correto):
dpad_up    = dpup
dpad_down  = dpdown
dpad_left  = dpleft
dpad_right = dpright